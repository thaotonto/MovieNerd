$("#create_room_data").data("map", <%= raw @support.map %>);
$("#create_room_data").data("deleted_seats",
  <%= raw @support.deleted_seats %>);

$("form_area").html(
  "<%= escape_javascript(render("form", support: @support)) %>");

$(document).trigger("turbolinks:load");
