<div id="block-form-<%= user.id %>">
  <%= form_for :block_users, url: admin_user_path(user), remote: true,
    html: {method: :patch} do |f| %>
    <div><%= hidden_field_tag :type, user.handle(1) %></div>
    <%= f.submit t(user.handle(2)), class: user.handle(3) %>
  <% end %>
</div>
