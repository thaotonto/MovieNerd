$("#room_show_data").data("sold", <%= raw @support.sold %>);
$("#room_show_data").data("selected_seats", <%= raw @selected_seats %>);

$("#flash_area").html("<%= j render("layouts/flash_message") %>");

$(document).trigger("turbolinks:load");
